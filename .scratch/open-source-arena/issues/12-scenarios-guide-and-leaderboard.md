# 12: Scenarios guide + LEADERBOARD.md committed + submission protocol

Status: ready-for-agent

## Parent

`.scratch/open-source-arena/PRD.md`

## What to build

Ship the remaining two root docs that depend on later slices:

- `SCENARIOS.md` — the scenario authoring protocol (`Scenario.apply(world, day)`), the override taxonomy (weather overrides, mutable pricing fields, injected named events, scenario trace), the dotted-path discovery convention, and a short profile of each v1 shipped scenario (baseline / grid-stress / economy-stress) with the stresses it applies.
- `LEADERBOARD.md` — the initial leaderboard committed to the repo, regenerated by the arena leaderboard tool from issue 07 against the baselines from issue 08.

Also document the submission protocol — community agents live under `agents/community/`, one Python file per submission, with a header docstring naming the author and summarizing the approach; the leaderboard is regenerated on merge and committed manually as part of (or just after) the merge.

## Acceptance criteria

- [ ] `SCENARIOS.md` documents the protocol, the override taxonomy, and each v1 scenario's stress profile, with code references to `scenarios/`.
- [ ] `LEADERBOARD.md` is generated by `arena/leaderboard.py` and committed; reproducible via the same command.
- [ ] `CONTRIBUTING.md` (from issue 11) references the community submission protocol; the protocol is described in `CONTRIBUTING.md` or `SCENARIOS.md` and discoverable from `README.md`.
- [ ] `make check` passes.

## Blocked by

- 05 (`v1 scenarios`)
- 08 (`baselines committed`)
