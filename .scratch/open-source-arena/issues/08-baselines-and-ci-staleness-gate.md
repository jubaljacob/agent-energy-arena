# 08: Baselines committed + Makefile regenerate target

Status: ready-for-agent

## Parent

`.scratch/open-source-arena/PRD.md`

## What to build

Commit per-scenario baseline files generated by running the scripted reference agent against every public scenario:

- One file per `(scenario, seed)` under `baselines/`, named to encode both. Each file is the committed JSON result row (population, treasury delta, renewable share, raw score) produced by the arena runner.
- A `Makefile` target `make baselines` that regenerates every baseline by running the scripted agent against every public scenario via the arena runner.

The hidden eval suite uses different seeds and is regenerated by the maintainer offline — it does not live in this repo.

A CI staleness gate is **out of scope for this slice** — the arena will get CI once it is more mature. Track that as a follow-up issue when the time comes.

## Acceptance criteria

- [ ] `baselines/` contains one committed JSON baseline file per (public scenario, seed) pair.
- [ ] `make baselines` regenerates every baseline.
- [ ] The end-to-end arena integration test from issue 07 reads against these committed baselines.
- [ ] `make check` passes.

## Blocked by

- 05 (`v1 scenarios`)
- 07 (`arena runner + leaderboard`)

## Comments

### Triage Notes

> *This was generated by AI during triage.*

**What we've established so far:**

- `baselines/seed_42.json` already exists in the working tree (current single-baseline state).
- `make baselines` target and the on-disk schema (one JSON per `(scenario, seed)`) are unambiguous.
- The end-to-end arena test from issue 07 will consume these committed files.

**Resolved:**

- CI staleness gate dropped from this slice (maintainer, 2026-05-13). Rationale: CI will be set up once the arena is more mature. The Makefile target and committed baselines remain in scope; a CI follow-up will be filed when CI is ready.
