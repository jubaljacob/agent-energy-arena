# 13: Packaging — PyPI distribution name update + egg-info cleanup

Status: ready-for-agent

## Parent

`.scratch/open-source-arena/PRD.md`

## What to build

Update the PyPI distribution name in `pyproject.toml` from `energy-ai-nexus` to `agent-energy-arena`. The Python package directories (`world`, `agents`) keep their current names — no imports change. Delete the stale `energy_ai_nexus.egg-info/` directory and add it (or a glob) to `.gitignore` if not already covered.

## Acceptance criteria

- [ ] `pyproject.toml`'s `name` field is set to `agent-energy-arena`.
- [ ] No Python `import` statements anywhere in the repo need to change.
- [ ] `energy_ai_nexus.egg-info/` is removed from the working tree.
- [ ] `*.egg-info/` is gitignored.
- [ ] `make check` passes.

## Blocked by

None — can start immediately.

## Comments

### Triage Notes

> *This was generated by AI during triage.*

**What we've established so far:**

- Current `pyproject.toml` distribution name is `energy-ai-nexus`.
- Stale `energy_ai_nexus.egg-info/` exists at the repo root and should be removed plus globbed in `.gitignore`.
- Python package directories (`world/`, `agents/`) stay untouched, so this is a metadata-only change.

**Resolved:**

- New PyPI distribution name: `agent-energy-arena` (maintainer, 2026-05-13). Reflected in "What to build" and acceptance criteria above.
